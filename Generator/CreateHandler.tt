<#@ template language="C#" #>
using AutoMapper;

namespace Sample.Generated {
    [IoC]
    public sealed class Create<#=_type.Name#>Handler : IHandler<Create<#=_type.Name#>Command>
    {
        private static readonly IMapper Mapper = 
            new MapperConfiguration(cfg =>
            {
                cfg.CreateMap<Create<#=_type.Name#>Command, <#=_type.Name#>Row>();
                cfg.CreateMap<Create<#=_type.Name#>Command, <#=_type.Name#>CreatedEvent>();
            })
            .CreateMapper();
		private readonly EventPublisher _publisher;
		private readonly <#=_type.Name#>Aggregate _aggregate;

		public Create<#=_type.Name#>Handler(
			EventPublisher publisher,
			<#=_type.Name#>Aggregate aggregate)
		{
			_publisher = publisher;
			_aggregate = aggregate;
		}
		public void Handle(Create<#=_type.Name#>Command command)
		{
			_aggregate.Create(Mapper.Map<<#=_type.Name#>Row>(command));
			_publisher.Publish(Mapper.Map<<#=_type.Name#>CreatedEvent>(command));
		}
    }
}

<#+
    private readonly MetaType _type;
#>