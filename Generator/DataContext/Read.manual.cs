using Microsoft.Data.Entity.Metadata;

namespace Generator.DataContext
{
    partial class Read
    {
        private readonly IModel _model;

        public Read(IModel model) {
            _model = model;
        }
    }
}