<UserControl x:Class="Sample.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Sample"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance local:MainViewModel}">
    <x:Code>
        <![CDATA[ public MainView() { InitializeComponent(); }]]>
    </x:Code>
    <StackPanel Orientation="Vertical">
        <Button x:Name="AddCities" Content="Add Cities"/>
        <Button x:Name="Load" Content="Load"/>
        <DataGrid x:Name="People" SelectedItem="{Binding SelectedPerson}"/>
        <Button x:Name="NewPerson" Content="New Person"/>
        <StackPanel Orientation="Horizontal" Margin="3">
            <TextBlock Text="Id:  "/>
            <TextBlock Text="{Binding SelectedPerson.Id}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="3">
            <TextBlock Text="Name:  "/>
            <TextBox Text="{Binding SelectedPerson.Name}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="3">
            <TextBlock Text="City:  "/>
            <ComboBox ItemsSource="{Binding Cities}" 
                      SelectedItem="{Binding SelectedPerson.City}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

        </StackPanel>
        <Button x:Name="Save" Content="Save"/>
    </StackPanel>
</UserControl>